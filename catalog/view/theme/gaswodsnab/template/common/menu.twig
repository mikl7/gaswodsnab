{# 
{% if categories %}
<div class="container">
  <nav id="menu" class="navbar">
    <div class="navbar-header"><span id="category" class="visible-xs">{{ text_category }}</span>
      <button type="button" class="btn btn-navbar navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse"><i class="fa fa-bars"></i></button>
    </div>
    <div class="collapse navbar-collapse navbar-ex1-collapse">
      <ul class="nav navbar-nav">
        {% for category in categories %}
        {% if category.children %}
        <li class="dropdown"><a href="{{ category.href }}" class="dropdown-toggle" data-toggle="dropdown">{{ category.name }}</a>
          <div class="dropdown-menu">
            <div class="dropdown-inner"> {% for children in category.children|batch(category.children|length / category.column|round(1, 'ceil')) %}
              <ul class="list-unstyled">
                {% for child in children %}
                <li><a href="{{ child.href }}">{{ child.name }}</a></li>
                {% endfor %}
              </ul>
              {% endfor %}</div>
            <a href="{{ category.href }}" class="see-all">{{ text_all }} {{ category.name }}</a> </div>
        </li>
        {% else %}
        <li><a href="{{ category.href }}">{{ category.name }}</a></li>
        {% endif %}
        {% endfor %}
      </ul>
    </div>
  </nav>
</div>
{% endif %} 
#}


<nav id="menu" class="navbar">
    <div class="menu">
        <ul class="catalogServices">
            <a href="#" class="catalog">
                <li>Каталог<img src="/catalog/view/theme/gaswodsnab/image/icons/arrow.png" alt="arrow"></li>
            </a>
            <a href="#">
                <li>Услуги<img src="/catalog/view/theme/gaswodsnab/image/icons/arrow.png" alt="arrow"></li>
            </a>
        </ul>
        <ul class="">
            <a href="">
                <li><img src="/catalog/view/theme/gaswodsnab/image/icons/compair.png" alt="compair">Сравнить</li>
            </a>
            <a href="{{ wishlist }}" id="wishlist-total" title="{{ text_wishlist }}">
              <li class="wishlist">
                  <img src="/catalog/view/theme/gaswodsnab/image/icons/bookmark.png" alt="bookmark">
                  <div>
                    <span>{{ text_wishlist }}</span>
                  </div>
                  Отложенные
              </li>
            </a>
            <a href="">
                {{ cart }}
            </a>
        </ul>    
    </div>
</nav>
